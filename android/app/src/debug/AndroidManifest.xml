<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG BUILD ONLY, merged only into debug builds by Gradle's src/debug source set,
     never into release. Points the debug application element at the debug-only cleartext
     exception above, without touching the release manifest's usesCleartextTraffic="false". -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
